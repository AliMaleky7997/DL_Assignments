import numpy as np

# import tensorflow as tf

# initial_value = tf.truncated_normal(shape=(1, 2, 3, 4), mean=0, stddev=0.01)
# x = tf.Variable(initial_value=initial_value)

print(type(np.prod(np.array([1,2,3]))))